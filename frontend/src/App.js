import GlobalStyleConfig from './config/globalStyleConfig';
import { Router } from './router/Router';

function App() {

	return (
		<>
			<GlobalStyleConfig/>
			<Router/>
		</>
	);
}

export default App;
